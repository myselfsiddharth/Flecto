# What `secrets.before.yaml` looks like when someone commits the decrypted file
# by mistake: no `sops` block, and the values in the clear. These particular
# values are invented for the fixture and are not credentials for anything.
service: billing-api
replicas: 3
database:
    host: db.internal
    username: billing_rw
    password: not-a-real-password
api:
    key: not-a-real-api-key
